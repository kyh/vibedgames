{
  "name": "@repo/party",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf .turbo node_modules .cache .wrangler",
    "deploy": "wrangler deploy --name vg-partyserver",
    "dev": "pnpm with-env wrangler dev",
    "typecheck": "tsc --noEmit",
    "with-env": "dotenv -e ../../.env --"
  },
  "prettier": "@kyh/prettier-config",
  "dependencies": {
    "@repo/api": "workspace:^",
    "@repo/db": "workspace:^",
    "@repo/multiplayer": "workspace:^"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260307.1",
    "@kyh/tsconfig": "catalog:",
    "partyserver": "^0.1.5",
    "typescript": "catalog:",
    "wrangler": "^4.71.0"
  }
}
