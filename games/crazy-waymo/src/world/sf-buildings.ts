// AUTO-GENERATED — currently EMPTY: the downtown dataset needs recalibration
// for the 488x400 world (tools/sf-data/calibrate-downtown.mjs; the 2x-scale
// road-correlation locks false optima and the Salesforce/Transamerica anchor
// pair mismatches — see project memory). Procedural frontage fills downtown
// meanwhile. Entries: [x, z, width, depth, height] world units.
export const SF_BUILDINGS: readonly (readonly [number, number, number, number, number])[] = [];

export const SF_BUILDINGS_BOUNDS = {
  minX: 1e9,
  maxX: 1e9,
  minZ: 1e9,
  maxZ: 1e9,
} as const;
