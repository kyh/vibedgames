// Stub for @mediapipe/pose — a closure-compiled IIFE without ESM exports.
// @tensorflow-models/pose-detection imports { Pose } for BlazePose, but this
// app only uses MoveNet. Providing a stub avoids the missing export error.
export const Pose = undefined;
