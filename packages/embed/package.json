{
  "name": "@repo/embed",
  "version": "0.0.1",
  "private": true,
  "description": "Game ↔ wrapper messaging for embedded browser games — start notifications, pause requests, and a built-in pause overlay",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./host": "./src/host.ts"
  },
  "scripts": {
    "clean": "rm -rf .cache .turbo dist node_modules",
    "test": "node --import tsx tools/pause-key-smoke.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@kyh/tsconfig": "catalog:",
    "tsx": "^4.23.12",
    "typescript": "catalog:"
  }
}
